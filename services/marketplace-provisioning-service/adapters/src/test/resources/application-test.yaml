spring:
  liquibase:
    enabled: true

marketplace:
  kafka:
    bootstrap-servers: ${spring.embedded.kafka.brokers}
    topic:
      dlq: order-paid-dlq

logging:
  level:
    root: INFO
    liquibase: DEBUG
    org.testcontainers: INFO
